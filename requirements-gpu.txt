# GPU-Accelerated Dependencies
# Install with: pip install -r requirements-gpu.txt
#
# Prerequisites:
# - NVIDIA GPU with CUDA support
# - CUDA 11.8 or 12.1 installed
# - cuDNN installed

# ============================================================================
# API and Backend Dependencies
# ============================================================================
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0
python-multipart==0.0.9
redis==5.0.1
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
mlflow==2.10.2
pint==0.23
pytest==8.0.0
pytest-cov==4.1.0
pytest-asyncio==0.23.4
httpx==0.26.0

# ============================================================================
# Core Scientific Computing
# ============================================================================
numpy==1.26.4
scipy==1.11.4
pandas==2.1.4

# ============================================================================
# Machine Learning Libraries (with GPU support)
# ============================================================================
scikit-learn==1.3.2

# Gradient Boosting with GPU support
xgboost==2.0.3  # GPU support via CUDA
lightgbm==4.1.0  # GPU support via CUDA or OpenCL
catboost==1.2.2  # GPU support via CUDA

# ============================================================================
# PyTorch with CUDA 11.8 support
# ============================================================================
# For CUDA 11.8 (recommended)
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2

# Alternative: For CUDA 12.1
# --extra-index-url https://download.pytorch.org/whl/cu121
# torch==2.1.2
# torchvision==0.16.2
# torchaudio==2.1.2

# ============================================================================
# TensorFlow with GPU support
# ============================================================================
# TensorFlow 2.12+ automatically includes GPU support if CUDA is available
tensorflow==2.15.0

# Alternative: Explicit GPU version (older TensorFlow)
# tensorflow-gpu>=2.8.0,<2.12.0

# ============================================================================
# NLP and Transformers
# ============================================================================
transformers==4.35.2
tokenizers==0.15.0
sentencepiece==0.1.99
accelerate>=0.20.0  # Hugging Face GPU acceleration

# ============================================================================
# Computer Vision
# ============================================================================
opencv-python==4.8.1.78
Pillow==10.1.0
albumentations==1.3.1

# ============================================================================
# Hyperparameter Optimization (GPU-aware)
# ============================================================================
optuna==3.4.0
ray[tune]==2.9.3
ray[default]==2.9.3

# ============================================================================
# Model Serving
# ============================================================================
onnx==1.15.0
# ONNX Runtime with GPU support
onnxruntime-gpu==1.16.3
joblib==1.3.2

# ============================================================================
# GPU Utilities and Monitoring
# ============================================================================
# NVIDIA GPU monitoring
nvidia-ml-py3>=7.352.0
gpustat>=1.0.0

# ============================================================================
# CUDA Accelerated Libraries (optional)
# ============================================================================
# cupy-cuda11x>=12.0.0  # NumPy-compatible array library on GPU
# cudf-cu11>=23.0.0  # GPU DataFrame library (requires RAPIDS)
# cugraph-cu11>=23.0.0  # GPU graph analytics

# ============================================================================
# Visualization
# ============================================================================
matplotlib==3.8.2
seaborn==0.13.0

# ============================================================================
# Utilities
# ============================================================================
tqdm==4.66.1
pyyaml==6.0.1

# ============================================================================
# Notes
# ============================================================================
# 1. For PyTorch, choose the CUDA version that matches your installed CUDA toolkit
# 2. TensorFlow 2.12+ automatically detects and uses GPU if CUDA is available
# 3. XGBoost, LightGBM, and CatBoost require specific compilation for GPU support
# 4. ONNX Runtime GPU requires CUDA and cuDNN to be installed
# 5. RAPIDS libraries (cuDF, cuGraph) require conda installation for best compatibility
