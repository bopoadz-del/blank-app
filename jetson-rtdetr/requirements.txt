# Core dependencies for RT-DETR TensorRT Inference
# Optimized for NVIDIA Jetson devices

# Image processing
numpy>=1.19.0
opencv-python>=4.5.0
Pillow>=8.0.0

# TensorRT (included with JetPack)
# tensorrt>=8.0.0  # Installed via JetPack
# pycuda>=2021.1   # Install manually on Jetson

# Optional: For ONNX export
# torch>=1.10.0
# torchvision>=0.11.0
# onnx>=1.10.0

# Development dependencies
pytest>=6.2.0
pytest-cov>=2.12.0

# Logging and utilities
tqdm>=4.62.0

# Note: On NVIDIA Jetson, install TensorRT via JetPack:
# sudo apt-get install nvidia-jetpack
#
# Install pycuda:
# pip3 install pycuda
#
# Verify installation:
# python3 -c "import tensorrt; print(tensorrt.__version__)"
# python3 -c "import pycuda.driver; print('PyCUDA OK')"
